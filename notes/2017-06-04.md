* ## **2017-06-04**

```javascript
[]+1=1;
[].valueOf().toString()=='' =>true
[]==0 =>true

```
运算符优先级
```
++              自增（前后）
--              自减（前后）
-               求反  例如： 1  ->  -1
+               转换为数字   +’1’ -> (Number)1
~               按位求反[附录1]
！              逻辑非   !false ->true
delete          删除属性   delete a.age 
typeof          检查操作数类型  typeof('123') -> string
void            返回undefined值
---------------------------------------------------------
* / %           乘 除 取余   2*2->4  2/2->1  2%2->0
---------------------------------------------------------
+ -             加法，减法   1+2 -> 3  3-2 -> 1
+               字符串拼接   'aaa' + 'bbb' -> 'aaabbb'
---------------------------------------------------------
<<
>>
>>>
---------------------------------------------------------
< > <= >=       【数字】小于 大于  小于等于（不大于） 大于等于（不小于）
< > <= >=       【ASCII】小于 大于  小于等于（不大于） 大于等于（不小于）
instanceof       测试对象类
in              测试属性是否存在
---------------------------------------------------------
==              判断相等    1=='1' -> true
!=              判断不等    1!='2' -> true
===             判断恒等    1==='1' -> false 
!==             判断不恒等   1!=='1' -> true
---------------------------------------------------------
&               按位与
---------------------------------------------------------
^               按位异或
---------------------------------------------------------
|               按位或
---------------------------------------------------------
&&              逻辑与 true && true ->true(同为true为true)
---------------------------------------------------------
||              逻辑或 true || false -> true(有true则为true)
---------------------------------------------------------
?:              条件运算  i<j? a=i : a=j  取最小值  
---------------------------------------------------------
=               赋值
*= /= += -= &=  运算且赋值
^= |= <<= >>= >>>=
---------------------------------------------------------
，              忽略第一个操作数，返回第二个操作数
---------------------------------------------------------
---------------------------------------------------------

```

附录：1
1. 按位取反 ~
```
var temp = ~5;
5 二进制 101，补满 32位
00000000000000000000000000000101
按位取反
11111111111111111111111111111010
由于32位开头第一个是1，所以这是一个负数，将二进制转换成负数，需要先反码
00000000000000000000000000000101
之后，再+1
00000000000000000000000000000110
转换成十进制为6，加上符号变成负数 -6
```